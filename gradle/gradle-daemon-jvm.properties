#Pins the JVM Gradle's daemon runs on to JDK 17, so Gradle's auto-discovery
#doesn't pick up a newer JDK (e.g. 25) whose version string the Kotlin DSL
#compiler bundled in this Gradle version can't parse.
toolchainVersion=17
